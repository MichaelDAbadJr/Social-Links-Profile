const SocialLink = ({ link }) => {
  return (
    <div className="social-media-links-container">
      <div className="social-media-links-button">{link}</div>
    </div>
  );
};

export default SocialLink;
